import React, { ReactElement } from 'react';
import './style.css';

const StationCard = (): ReactElement => {
  return (
    <div>
      <div className="container">this is StationCard!</div>
    </div>
  );
};

export default StationCard;
